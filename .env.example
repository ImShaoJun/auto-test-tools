# Optional: Set any environment variables your backend might need for testing
# ANTHROPIC_API_KEY is no longer required by the Karate MCP Server itself,
# as Claude Code acts as the sole LLM brain and handles all text generation.
